module.exports = {
   purge: ['./src/**/*.tsx'],
   darkMode: false, // or 'media' or 'class'
   theme: {
      fontFamily: {
         body: ['Poppins'],
      },
      extend: {},
   },
   variants: {
      extend: {},
   },
   plugins: [],
}
